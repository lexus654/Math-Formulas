// Given a and b find c
// work on this so that the program can find 1 unknown
const valueA = prompt(alert("Insert the value of A"));
const valueB = prompt(alert("Insert the value of B"));
const valueC = Math.sqrt(valueA ** 2 + valueB ** 2);
console.log(valueC);
